class Solution {
    public int climbStairs(int n) {
        if (n <= 2) return n;
        int total = 0;
        int x = 1;
        int y = 2;

        for (int i = 3; i <= n; i++) {
            total = x + y;
            x = y;
            y = total;
        }

        return total;

    }
}
